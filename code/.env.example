# Required: base URL of the FastAPI backend (no trailing slash)
# For local development, copy this file to .env.local and set the value.
# For production (GitHub Pages), set this as an environment variable in your deployment platform.
VITE_API_URL=https://your-api-url.com
